// GENERATED by `npm run gap:generate` from requirement SEARCH-002.
// Regenerating does not overwrite this file automatically — remove it first for a fresh version.
import { test, expect } from '../../../../../src/core/fixtures/base.fixture';
import { TAGS } from '../../../../../src/core/constants';
import { loadDataProfile } from '../../../../../src/core/execution/data-profile';
import { getExecutionContext } from '../../../../../src/core/execution/execution-context';

const profile = loadDataProfile('amazon', getExecutionContext().dataProfile ?? 'qa-default');

test.describe('Amazon Search (generated)', () => {
  test(
    'user can search for a product',
    {
      tag: [
        '@application.amazon',
        '@module.search',
        TAGS.FUNCTIONAL,
        '@generated',
        '@amazon.search.generated.2',
        '@requirement.SEARCH-002',
      ],
    },
    async ({ page, ui }) => {
      await page.goto('/gp/yourstore');
      await ui.fill('Search Amazon', 'laptop');
      await ui.click('Go');
    },
  );
});
